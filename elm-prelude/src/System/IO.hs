{-# LANGUAGE PackageImports #-}

module System.IO
  ( BufferMode(..)
  , hFlush
  , hSetBuffering
  , stderr
  , stdin
  , stdout
  ) where

import           "base" System.IO (BufferMode (..), hFlush, hSetBuffering,
                                   stderr, stdin, stdout)
